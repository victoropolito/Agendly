packages:
  - apps/*
  - packages/*

# `unrs-resolver` has no required runtime build step for this project. Pin the
# decision explicitly so pnpm does not ask for an interactive approval in CI.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  argon2: true
  prisma: true
  unrs-resolver: false
